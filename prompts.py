PROMPTS = {
    "p1": "Generate an interesting and elaborate MongoDB aggregation pipeline for performance benchmarking, potentially including constructs such as $lookup (joins), nested pipelines (subquery-like behavior), multi-stage transformations, $group, $setWindowFields, $facet, complex $match predicates, computed expressions, string manipulations, and handling of missing or null fields. Return only the aggregation pipeline as a JSON array of stage objects.",
    "p2": "Generate an interesting and elaborate MongoDB find query for performance benchmarking. Return a JSON object with two keys: 'filter' (the query predicate) and 'projection' (the fields to include or exclude).",
    "p3": "Generate a MongoDB find query suitable for performance benchmarking. Return a JSON object with two keys: 'filter' (the query predicate) and 'projection' (the fields to include or exclude).",
    "p4": "Generate a simple MongoDB aggregation pipeline for benchmarking, using basic constructs such as $match, $project, $sort, and $limit. Return only the aggregation pipeline as a JSON array of stage objects.",
    "p5": "Generate an interesting and elaborate MongoDB aggregation pipeline for performance benchmarking, potentially including $lookup, nested pipelines, $group, $setWindowFields, $facet, complex expressions, string operations, and handling of null or missing fields. Incorporate obscure semantic corner cases, unusual operator combinations, edge cases in array handling, type inconsistencies, and non-obvious query behaviors. Return only the aggregation pipeline as a JSON array of stage objects.",
    "p6": "Generate an interesting and elaborate MongoDB find query for benchmarking string processing, using operators such as $regex and text-based filtering. Return a JSON object with two keys: 'filter' (the query predicate, making heavy use of string operators) and 'projection' (the fields to include or exclude).",
    "p7": "Generate an interesting and elaborate MongoDB aggregation pipeline for performance benchmarking, potentially including $lookup, $graphLookup (recursive traversal), nested pipelines, $setWindowFields, $group, $facet, complex expressions, string processing, and handling of null or missing values. Aim to maximize structural and computational complexity. Return only the aggregation pipeline as a JSON array of stage objects.",
}